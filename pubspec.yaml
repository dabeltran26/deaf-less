name: deaf_less
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  equatable: ^2.0.5
  go_router: ^14.2.7
  get_it: ^7.7.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^5.0.0
  # Optional: hive_generator for future type adapters
  # build_runner:

flutter:
  uses-material-design: true
  # iOS: add NSMicrophoneUsageDescription in Info.plist
  # Android: ensure android.permission.RECORD_AUDIO is declared in AndroidManifest.xml
  assets:
    - assets/tokenizer.json
    - assets/modelo.pte
    - assets/sample_audio.wav
